/**
 * Button 컴포넌트 모듈이에요.
 * 컴포넌트, Props 타입, 스타일 타입을 export해요.
 *
 * @example
 * ```tsx
 * import { Button, type ButtonProps, type ButtonColors } from "@harryk-ds/ui";
 * ```
 *
 * @module
 */

export type { ButtonProps } from "./button";
export { Button } from "./button";
export type { ButtonColors, ButtonSizes, ButtonVariants } from "./button.types";
