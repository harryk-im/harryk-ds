---
"@harryk-ds/ui": minor
---

Realign the `COLORS` ramps to a lightness-first scale: `blue`, `red`, and `grey` share one core ramp so the same step has the same lightness across families, and `lightGrey` becomes a background-only surface ramp. No token names changed, but most values did — expect a visible color shift.
