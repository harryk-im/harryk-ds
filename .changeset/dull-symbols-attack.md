---
"@harryk-ds/ui": minor
---

Refine the `Button` styles: shift `fill` backgrounds and `weak` text one step darker (`400` and `300`), replace the white `weak` background with translucent `lightGrey`, and scale the border radius with `size` (`sm` 8px, `md` 12px, `lg` 16px) instead of a fixed 16px. No props changed — expect a visible style shift.
