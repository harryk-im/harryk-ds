---
"@harryk-ds/ui": minor
---

Redesign the `Button` component to match the updated design system: replace semantic `color` values (`primary`/`secondary`) with primitives (`blue`/`red`/`grey`), swap the `outline` variant for `weak`, remove borders and built-in animation, and add `fullWidth` and `loading` props.
