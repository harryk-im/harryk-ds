---
"@harryk-ds/ui": patch
---

Add Modal compound component (`Header` / `Body` / `Footer`) with `sm`/`md`/`lg` sizes, portal rendering, overlay dismiss, and companion hooks (`useDisclosure`, `useCloseOnEsc`, `useScrollLock`).
